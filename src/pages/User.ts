export class Users{
    FirstName: string;
    Surname: string;
    Identity: string;

    constructor(FirstName:string, Surname:string, Identity){
     this.FirstName = FirstName;
     this.Surname = Surname;
     this.Identity = Identity;
    }
}